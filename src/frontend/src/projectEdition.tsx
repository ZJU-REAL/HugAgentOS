import type { ProjectItem } from './types';
import type { EditionProjectUpdateFields } from './editionModelTypes';

export function ProjectVisibilityEditor(_props: {
  project: ProjectItem;
  onPatchChange: (patch: EditionProjectUpdateFields) => void;
}) {
  return null;
}
